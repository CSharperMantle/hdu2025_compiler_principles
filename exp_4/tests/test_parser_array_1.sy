const int N = 3;
int global_arr[2][3] = {{1,2,3},{4,5,6}};

int main() {
    int local_arr[2][N] = {{1,2,3},{4,5,6}};
    int sum = 0;
    
    sum = sum + global_arr[0][1] * local_arr[1][0];
    
    if (sum > 10) {
        return sum;
    }
    return 0;
}